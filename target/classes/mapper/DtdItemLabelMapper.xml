<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.annotation.dao.DtdItemLabelMapper" >
    <resultMap id="BaseResultMap" type="com.annotation.model.DtdItemLabel" >
        <result column="dt_inst_id" property="dtInstId" jdbcType="INTEGER" />
        <result column="labeltype" property="labeltype" jdbcType="VARCHAR" />
        <result column="label_id" property="labelId" jdbcType="INTEGER" />
    </resultMap>

    <insert id="insert" parameterType="com.annotation.model.DtdItemLabel" >
    insert into dtd_item_label (dt_inst_id, labeltype, label_id)
    values (#{dtInstId,jdbcType=INTEGER}, #{labeltype,jdbcType=VARCHAR}, #{labelId,jdbcType=INTEGER}
    )
    </insert>

    <select id="selectAll" resultMap="BaseResultMap" >
    select dt_inst_id, labeltype, label_id
    from dtd_item_label
    </select>

    <select id="selectByAllAttr" resultMap="BaseResultMap" >
        select dt_inst_id, labeltype, label_id from dtd_item_label
          where dt_inst_id=#{dtInstId} AND labeltype=#{labeltype} AND label_id=#{labelid}
    </select>

    <insert id="insertLabelList">
        insert into dtd_item_label (dt_inst_id, labeltype, label_id) values
        <foreach collection="itemLabels" index="index" item="item" separator=",">
            ( #{dtInstId}, #{labeltype},#{item} )
        </foreach>
        ON DUPLICATE KEY UPDATE labeltype = values(labeltype)
    </insert>

</mapper>